(jbuild_version 1)

(library
  ((name Cafec_Parse)
  (libraries (base ppx_let Cafec_Containers uutf uucp))
  (preprocess (pps (ppx_let)))
  (flags (:standard -w +a-4-41@8 -open Base -open Cafec_Containers))))
