(executables
  (names main)
  (libraries
    base
    stdio
    Cafec)
  (flags :standard -safe-string -w @a-4 -open Base -open Cafec_Containers))

(install
  (section bin)
  (package cafec)
  (files (main.exe as cafec)))
