(jbuild_version 1)

(executables
 ((names (main))
  (libraries (pred cafec_parse cafec_spanned cafec_typed_ast))
  (flags (:standard -w @a))))

(install
 ((section bin)
  (package cafec)
  (files ((main.exe as cafec)))))
