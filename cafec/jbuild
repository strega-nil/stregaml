(jbuild_version 1)

(executables
 ((names (main))
  (libraries (pred cafec_parse cafec_spanned cafec_typed_ast cafec_interpreter))
  (flags (:standard -w +a-4 -open Pred))))

(install
 ((section bin)
  (package cafec)
  (files ((main.exe as cafec)))))
