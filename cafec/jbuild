(jbuild_version 1)

(executables
 ((names (main))
  (libraries (pred parse spanned untyped_ast typed_ast))
  (flags (:standard -w @a))))

(install
 ((section bin)
  (package cafec)
  (files ((main.exe as cafec)))))
